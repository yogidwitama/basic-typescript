"use strict";
describe('Data type', function () {
    it('should must declare', function () {
        const name = "Yogi Dwitama";
        const balance = 9000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
