"use strict";
describe('Union Type', () => {
    it("should support in typescript", function () {
        let sample = "yogi";
        console.info(sample);
        sample = 100;
        console.info(sample);
        sample = true;
        console.info(sample);
    });
});
